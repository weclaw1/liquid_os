language: rust

rust:
  - nightly

cache:
  - cargo

install:
  - which xargo || cargo install xargo
  - which just || cargo install just
  - rustup component add rust-src

script:
  - just kernel
  - just test

addons:
  apt:
    packages:
      - xorriso
      - nasm
      - qemu
      - build-essential
      - mtools
